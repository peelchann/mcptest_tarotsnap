/**
 * AmbientBackground — full-screen layered radial gradients + smoke blobs.
 * pointer-events:none and z-0; sits behind everything.
 */
export default function AmbientBackground() {
  return <div className="ambient-bg" aria-hidden="true" />;
}
